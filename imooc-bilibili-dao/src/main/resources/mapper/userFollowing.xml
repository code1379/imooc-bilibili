<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.bilibili.dao.UserFollowingDao">
    <select id="deleteUserFollowing" resultType="java.lang.Long">
        delete from
                   t_user_following
        where
            userId = #{userId} and followingId = #{followingId}
    </select>
    <insert id="addUserFollowing" parameterType="com.imooc.bilibili.domain.UserFollowing">
        insert into
            t_user_following(
            userId,
            followingId,
            groupId,
            createTime
        ) values(
                    #{userId},
                    #{followingId},
                    #{groupId},
                    #{createTime}
                )
    </insert>
</mapper>